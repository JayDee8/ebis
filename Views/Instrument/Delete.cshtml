@model ebis.Models.nastroje

@{
    ViewBag.Title = "Odebrat nástroj";
}

<h3>Opravdu chcete odebrat nástroj @Model.jmeno?</h3>

@using (Html.BeginForm()) {
    <p>
        @Html.Bootstrap().SubmitButton().Style(ButtonStyle.Danger).Text("Odebrat") |
        @Html.ActionLink("Zpět na seznam", "Index")
    </p>
}
