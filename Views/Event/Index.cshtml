@model IEnumerable<ebis.Models.akce>

@{
    ViewBag.Title = "Akce";
    WebGrid grid = new WebGrid(Model);
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Přidat novou akci", "Create")
</p>
@grid.GetHtml(columns:
    grid.Columns
    (
        grid.Column("jmeno", "Název"),
        grid.Column("popis", "Popis"),
        grid.Column("datum", "Datum"),
        grid.Column("cas", "Čas"),
        grid.Column(format: (item) => new HtmlString(Html.ActionLink("Detaily", "Details", new { id = item.pk_id }) + " | " +
                    Html.ActionLink("Upravit", "Edit", new { id = item.pk_id }) + " | " +
                    Html.ActionLink("Smazat", "Delete", new { id = item.pk_id })))
        ))
