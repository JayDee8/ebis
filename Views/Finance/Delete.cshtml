@model ebis.Models.naklady

@{
    ViewBag.Title = "Odebrat náklady";
}

<h3>Opravdu chcete odebrat náklady @Model.jmeno?</h3>

@using (Html.BeginForm()) {
    <p>
        @Html.Bootstrap().SubmitButton().Style(ButtonStyle.Danger).Text("Odebrat") |
        @Html.ActionLink("Zpět na seznam", "Index")
    </p>
}
