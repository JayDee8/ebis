@model ebis.Models.naklady

@{
    ViewBag.Title = "Odebrat náklady";
}

<h2>@ViewBag.Title</h2>

<h3>Opravdu chcete odebrat tyto náklady?</h3>
<fieldset>
    <legend>naklady</legend>

    <div class="display-label">
         Název
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.jmeno)
    </div>

    <div class="display-label">
         Type
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.typ)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Odebrat" /> |
        @Html.ActionLink("Zpět na seznam", "Index")
    </p>
}
